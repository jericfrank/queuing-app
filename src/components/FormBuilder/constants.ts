import { SupportedFormField } from "./types";

export const SUPPORTED_FORM_FIELDS: SupportedFormField[] = [
  {
    name: 'Text Field',
    type: 'text',
  },
  {
    name: 'Dropdown',
    type: 'select',
  },
];

export const FORM_FIELD_PROPERTY = 'formFields';